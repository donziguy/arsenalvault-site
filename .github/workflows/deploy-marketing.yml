name: Deploy Marketing Site

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy homepage to Hostinger
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 46.202.198.30
          port: 65002
          username: u421117894
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          timeout: 120s
          script: |
            set -e
            cd ~/domains/arsenalvault.com/public_html
            cp index.html index.html.bak.$(date +%Y%m%d%H%M%S)
            curl -fsSL "https://raw.githubusercontent.com/donziguy/arsenalvault-site/main/index.html?deploy=$(date +%s)" -o index.html
            grep -n "All plans include a 15-day free trial" index.html
            rm -f legal-new.html
